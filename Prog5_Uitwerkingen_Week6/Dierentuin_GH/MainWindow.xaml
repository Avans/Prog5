<Window x:Class="Dierentuin_GH.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Dierentuin_GH.ViewModel"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding Main, Source={StaticResource Locator}}">


    <StackPanel>

        <Button Command="{Binding ShowAddDierCommand}">+</Button>
        <DataGrid ItemsSource="{Binding Dieren}" SelectedItem="{Binding  SelectedDier}">

        </DataGrid>

        <Button Command="{Binding ShowDetailsCommand}">Details</Button>


    </StackPanel>
</Window>
